CONTEXT:
- Most developers are on Windows
- Developers already have Python installed
- There is an existing requirements.txt for prod dependencies
- We want hooks to run automatically on every git commit locally
- Hook code should NOT affect prod or CI/CD (CI/CD integration is out of scope for now)

FILES TO CREATE:
1. requirements-dev.txt
   - Add: pre-commit>=3.6.0

2. setup_dev.py
   - Cross-platform Python script (works on Windows/Mac/Linux)
   - Installs requirements-dev.txt
   - Runs: pre-commit install
   - Prints clear success message

3. .pre-commit-config.yaml
   - Register the two custom hooks below
   - language: python, types: [python]

4. hooks/check_file_naming.py
   - [PLACEHOLDER — rules to be defined later]
   - For now: always passes, prints "file naming check: skipped (rules pending)"

5. hooks/check_job_structure.py
   - [PLACEHOLDER — rules to be defined later]
   - For now: always passes, prints "job structure check: skipped (rules pending)"

6. Update README.md
   - Add a "Developer Setup" section
   - Instructions: clone repo → run python setup_dev.py → done

STRUCTURE:
your-repo/
├── .pre-commit-config.yaml
├── requirements.txt          ← do NOT touch, prod only
├── requirements-dev.txt      ← add pre-commit here
├── setup_dev.py              ← cross platform one-command setup
├── hooks/
│   ├── check_file_naming.py
│   └── check_job_structure.py
